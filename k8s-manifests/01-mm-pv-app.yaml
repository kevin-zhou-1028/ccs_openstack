apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mm-app
spec:
  capacity:
    storage: 10Gi
  accessModes: ["ReadWriteOnce"]
  storageClassName: ""
  persistentVolumeReclaimPolicy: Retain
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: storage
          operator: In
          values: ["mattermost"]
  hostPath:
    path: /mnt/mattermost/app
