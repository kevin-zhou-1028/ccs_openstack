apiVersion: v1
kind: PersistentVolume
metadata:
  name: mm-plugins-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: /var/lib/mm-plugins
